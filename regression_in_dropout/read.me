# Dropout-Regression-ANN
# Dropout Layers in ANN - Regression Only 

## Overview
Dropout prevents overfitting in regression ANNs by randomly dropping 20-50% neurons during training. Disabled at inference for full predictions.

## Regression Code Example
